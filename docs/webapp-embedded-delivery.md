# Embedded Webapp Delivery

The maintained build, partition, asset-serving and update contract is
[ESP32 embedded delivery](../webapp/docs/embedded-delivery.md).
The [WebUI README](../webapp/README.md) contains clean-checkout browser commands.

This index replaces duplicate delivery guidance so budgets and release rules
have one source. Current operation is build-only; uploads and OTA are not enabled.
